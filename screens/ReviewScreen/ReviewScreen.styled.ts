import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    goBackButton: {
        marginBottom: 24
    },
    title: {
        fontSize: 24,
        lineHeight: 33,
        marginBottom: 24,
        textAlign: 'left'
    },
  });